<!-- 

    Promise.all() 
    - 用于处理多个Primise对象，并在它们都完成后返回一个新的Promise

    Promise.all() 接收一个 Promise 对象数组作为参数，并返回一个新的 Promise。这个新的 Promise 在传入的所有 Promise 对象都成功完成后才会被解析，解析值是一个包含所有 Promise 结果的数组。如果任何一个 Promise 对象被拒绝（rejected），那么新的 Promise 将立即被拒绝，并返回拒绝的原因。

    Promise的写法自己应该清楚
 -->
